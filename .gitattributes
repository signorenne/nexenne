# Normalize line endings to LF in the repo, regardless of contributor OS.
* text=auto eol=lf

# Always-LF scripts (a CRLF shebang line breaks them).
*.sh text eol=lf

# Treat docs as documentation for language stats.
*.org linguist-documentation
