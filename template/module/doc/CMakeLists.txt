#
# Per-module docs for nexenne::__MODULE__.
# Run with: cmake --build <build> --target nexenne___MODULE___docs
# (or build everything at once via the aggregate: nexenne_docs)
#
nexenne_add_module_docs(__MODULE__)
