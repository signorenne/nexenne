#
# Per-module docs for nexenne::logging.
# Run with: cmake --build <build> --target nexenne_logging_docs
# (or build everything at once via the aggregate: nexenne_docs)
#
nexenne_add_module_docs(logging)
