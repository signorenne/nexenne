#
# Per-module docs for nexenne::chrono.
# Run with: cmake --build <build> --target nexenne_chrono_docs
# (or build everything at once via the aggregate: nexenne_docs)
#
nexenne_add_module_docs(chrono)
