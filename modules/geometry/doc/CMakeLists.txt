#
# Per-module docs for nexenne::geometry.
# Run with: cmake --build <build> --target nexenne_geometry_docs
# (or build everything at once via the aggregate: nexenne_docs)
#
nexenne_add_module_docs(geometry)
