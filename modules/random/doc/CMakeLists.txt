#
# Per-module docs for nexenne::random.
# Run with: cmake --build <build> --target nexenne_random_docs
# (or build everything at once via the aggregate: nexenne_docs)
#
nexenne_add_module_docs(random)
