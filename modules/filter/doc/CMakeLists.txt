#
# Per-module docs for nexenne::filter.
# Run with: cmake --build <build> --target nexenne_filter_docs
# (or build everything at once via the aggregate: nexenne_docs)
#
nexenne_add_module_docs(filter)
