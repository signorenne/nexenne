#
# Per-module docs for nexenne::signal.
# Run with: cmake --build <build> --target nexenne_signal_docs
# (or build everything at once via the aggregate: nexenne_docs)
#
nexenne_add_module_docs(signal)
