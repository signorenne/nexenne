#
# Per-module docs for nexenne::serialization.
# Run with: cmake --build <build> --target nexenne_serialization_docs
# (or build everything at once via the aggregate: nexenne_docs)
#
nexenne_add_module_docs(serialization)
