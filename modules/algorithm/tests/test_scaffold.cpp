/**
 * @file
 * @brief Scaffold smoke test for nexenne::algorithm.
 *
 * Placeholder while the module is under construction: it only confirms the
 * umbrella header includes and the test pipeline is wired. It is replaced by
 * the per-category suites (test_binary_search, test_hash, test_checksum, ...)
 * as each phase lands.
 */

#include <doctest/doctest.h>

#include <nexenne/algorithm/algorithm.hpp>

TEST_CASE("algorithm module scaffold compiles") {
  CHECK(true);
}
