#
# Per-module docs for nexenne::algorithm.
# Run with: cmake --build <build> --target nexenne_algorithm_docs
# (or build everything at once via the aggregate: nexenne_docs)
#
nexenne_add_module_docs(algorithm)
