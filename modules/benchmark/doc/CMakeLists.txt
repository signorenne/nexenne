#
# Per-module docs for nexenne::benchmark.
# Run with: cmake --build <build> --target nexenne_benchmark_docs
# (or build everything at once via the aggregate: nexenne_docs)
#
nexenne_add_module_docs(benchmark)
